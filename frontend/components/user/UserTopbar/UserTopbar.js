/**
 * UserTopbar.js
 * Componente de topbar para panel de usuario.
 * Muestra el logo, nombre del usuario y botones de acción.
 */

export async function mountUserTopbar(root, userName) {
  // Este componente se monta a través de UserShell
  // Ver: components/UserShell/UserShell.js
}
